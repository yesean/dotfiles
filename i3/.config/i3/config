set $mod Mod4
set $term alacritty

# Define names for default workspaces for which we configure key bindings later on.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

set $monitor-one "DP-0.8"
set $monitor-two "DP-0.1.8"

# set colors
set $nord0  #2e3440
set $nord1  #3b4252
set $nord2  #434c5e
set $nord3  #4c566a
set $nord4  #d8dee9
set $nord5  #e5e9f0
set $nord6  #eceff4
set $nord7  #8fbcbb
set $nord8  #88c0d0
set $nord9  #81a1c1
set $nord10 #5e81ac
set $nord11 #bf616a
set $nord12 #d08770
set $nord13 #ebcb8b
set $nord14 #a3be8c
set $nord15 #b48ead

# style window decoration colors
client.focused              $nord9 $nord9 $nord6 $nord3 $nord3
client.focused_inactive     $nord9 $nord9 $nord6 $nord3 $nord3
client.unfocused            $nord0 $nord0 $nord6 $nord3 $nord3
client.urgent               $nord12 $nord12 $nord6 $nord3 $nord3
client.placeholder          $nord0 $nord0 $nord6 $nord3 $nord3
client.background           $nord0

# set cursor size
exec --no-startup-id xrdb ~/.Xresources

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id /usr/bin/pulseaudio-ctl up && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id /usr/bin/pulseaudio-ctl down && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id /usr/bin/pulseaudio-ctl mute && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id /usr/bin/pulseaudio-ctl mute-input && $refresh_i3status

# Media player controls
bindsym XF86AudioPause exec --no-startup-id playerctl play-pause
bindsym XF86AudioNext exec --no-startup-id playerctl next
bindsym XF86AudioPrev exec --no-startup-id playerctl previous

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec --no-startup-id $term

# start web browser
bindsym $mod+shift+Return exec --no-startup-id brave

# start file system
bindsym $mod+mod1+Return exec --no-startup-id $term -e ranger

# kill focused window
bindsym $mod+q kill

# only change focus with keyboard
focus_follows_mouse no

# rofi
bindsym $mod+d exec --no-startup-id ~/.config/rofi/scripts/launch-search.sh
# rofi calculator
bindsym $mod+Shift+d exec --no-startup-id ~/.config/rofi/scripts/launch-calculator.sh

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+Shift+v split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+Shift+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# toggle scratchpad (used to minimize windows)
bindsym $mod+Shift+x [floating] move scratchpad
bindsym $mod+minus scratchpad show
bindsym $mod+x move scratchpad

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1; workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2; workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3; workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4; workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5; workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6; workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7; workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8; workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9; workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10; workspace number $ws10

# make certain windows floating by default
for_window [class="zoom" title="Zoom - Licensed Account"] floating enable, move absolute position center
for_window [class="zoom" title="Settings"] floating enable, move absolute position center
for_window [class="zoom" title="Chat"] floating enable, move absolute position center
for_window [class="zoom" title="Participants"] floating enable, move absolute position center

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec --no-startup-id "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will grow the window’s width.
        # Pressing right will shrink the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize grow width 5 px or 5 ppt
        bindsym j resize shrink height 5 px or 5 ppt
        bindsym k resize grow height 5 px or 5 ppt
        bindsym l resize shrink width 5 px or 5 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize grow width 5 px or 5 ppt
        bindsym Down resize shrink height 5 px or 5 ppt
        bindsym Up resize grow height 5 px or 5 ppt
        bindsym Right resize shrink width 5 px or 5 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

# bind moving workspaces shortcuts
bindsym $mod+Ctrl+h move workspace to output left
bindsym $mod+Ctrl+l move workspace to output right

# bind resizing gaps shortcuts
bindsym $mod+i        gaps inner current plus 10
bindsym $mod+Shift+i  gaps inner current minus 10
bindsym $mod+o        gaps outer current plus 10
bindsym $mod+Shift+o  gaps outer current minus 10

# save workspaces shortcut
bindsym $mod+Ctrl+s exec --no-startup-id ~/.config/i3/scripts/save-workspaces.sh

# flameshot shortcut (for screenshotting)
bindsym $mod+Ctrl+4 exec --no-startup-id flameshot gui
bindsym $mod+Ctrl+3 exec --no-startup-id flameshot full -c

# set gaps
gaps inner 10
gaps outer 0
gaps top 22

# set gaps for aesthetic workspace (10)
# workspace $ws10 gaps inner 20
# workspace $ws10 gaps outer 60
# workspace $ws9  gaps inner 20
# workspace $ws9  gaps outer 60
# workspace $ws8  gaps inner 20
# workspace $ws8  gaps outer 60

# style window title
font pango:Inconsolata 10
title_align center
for_window [class=".*"] title_format "    %title    "
for_window [class=".*"] border pixel 0

# load wallpaper
exec --no-startup-id ~/.fehbg &

# set correct monitor arrangement
# exec --no-startup-id ~/.config/i3/scripts/load-dual-monitors-arrangement.sh

# assign workspace to specific monitors
workspace $ws1  output $monitor-one
workspace $ws8  output $monitor-two
workspace $ws9  output $monitor-two
workspace $ws10 output $monitor-two

# start polybar
exec_always --no-startup-id ~/.config/polybar/launch.sh --blocks

# enable wake on sleep for mouse and keyboard
exec_always --no-startup-id sudo /home/sean/bin/enable-usb-wakeup.sh

# power cycle webcam
exec --no-startup-id sudo /home/sean/bin/reset-webcam.sh

# add virtual webcam
exec --no-startup-id sudo /home/sean/bin/add-virtual-webcam.sh

# start gnome-key-daemon
exec --no-startup-id eval $(/usr/bin/gnome-keyring-daemon --start --components=pkcs11,secrets,ssh)
exec --no-startup-id export SSD_AUTH_SOCK

# start dunst
exec_always --no-startup-id dunst

# sleep after idle for 30 minutes
exec --no-startup-id xidlehook --not-when-audio --not-when-fullscreen --timer 1800 'systemctl suspend' '' &

# restart picom
exec_always --no-startup-id killall picom
exec_always --no-startup-id picom --experimental-backends -b

# startup basic layout (ws 8,9,10)
exec --no-startup-id ~/.config/i3/scripts/load-default-workspaces.sh

# start insync
exec --no-startup-id insync start --no-daemon
